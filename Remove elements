class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        for i in range(1, nums.count(val)+1):
            if val in nums:
                nums.remove(val)
        return len(nums)
        
