
bool isPalindrome(int x){

    if(x<0)
        return 0;
    long long int z=x;
    long long int rem;
    long long int y=0;
    while(z)
    {
        y = y * 10;
        y = y + z%10;
        z = z/10;
    }
    if(y==x)
        return 1;
    else
        return 0;
}
